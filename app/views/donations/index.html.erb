<h1>Donations</h1>

<ul>
  <% @donations.each do |donation| %>
    <li>
      $<%= donation.amount %>

      <%= render partial: 'link', locals: { donation: donation } %>

      <%= donation.created_at.strftime(" on %B %e, %Y") %>
    </li>
  <% end %>
</ul>