<%= form_for @donation do |f| %>

  <div>
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div>
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <%= f.hidden_field :species_id, value: params[:id] %>

<% end %>