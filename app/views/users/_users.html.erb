<div class="index-header">
  <h1>Our <%= phrase %></h1>
</div>

<ul>
  <% users.each do |user| %>
    <li class="user-li"><%= link_to  user.username, user_path(user), class: "user-a" %></li>
  <% end %>
</ul>