<main class="wavy-image exhibit-bg-size">
  <div class="show-container">

    <div class="show-header">
      <h1><%= @exhibit.name %></h1>
    </div>

    <img src="<%= @exhibit.image_url %>" alt="">

    <div class="show-sub-container">
      <h2>Biome: <%= @exhibit.biome %></h2>
    </div>

    <div class="show-sub-container">
      <h3><%= @exhibit.description %></h3>
    </div>

    <div class="show-link-container">
      <%= link_to  "Animals", exhibit_animals_path(@exhibit), class: "button button3" %>
      <%= link_to  "Species", exhibit_species_index_path(@exhibit), class: "button button2" %>

      <% if is_admin %>
        <%= link_to  "Edit Exhibit", edit_exhibit_path(@exhibit), class: "button edit" %>
        <%= link_to  "Delete Exhibit", exhibit_path(@exhibit), method: :delete, class: "button delete" %>
      <% end %>
    </div>
  </div>
</main>
